<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <ImageView
        android:id="@+id/logo"
        style="@style/logo"
        />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@id/logo"
        android:orientation="vertical">

    <TextView
        android:id="@+id/aplikasi"
        style="@style/aplikasi"
        />

    <TextView
        android:id="@+id/aplikasi_info"
        style="@style/aplikasi_info"
        />

    <TextView
        android:id="@+id/owner"
        style="@style/owner"
        />

    <Button
        android:id="@+id/tombolinfo"
        style="@style/tombolinfo"
        />

    </LinearLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:layout_marginTop="180dp"
        android:id="@+id/list_maskapai"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:listitem="@layout/listmaskapai"
        />

</RelativeLayout>